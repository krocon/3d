{
  "name": "3d-model-viewer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "i": "npm install",
    "start": "node scripts/generate-thumbs.js && node src/server.js",
    "dev": "vite",
    "generate-thumbs": "node scripts/generate-thumbs.js",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
